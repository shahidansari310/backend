<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Database</title>
    <link rel="stylesheet" href="./stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen">
     <div class="w-full h-full bg-zinc-800 p-10 text-white">
        <div class="nav mb-10">
            <a href="/" class="text-blue-500">Home</a>
        </div>
            <h1 class="text-3xl tracking-tighter mb-5">All Users</h1>
            <div class="users flex gap-3 flex-wrap">
                <% if(user.length>0){ %>
                    <% user.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-700 rounded-lg">
                    <div class="w-full h-72 bg-zinc-500 rounded-lg">
                        <img class="w-full h-full object-position-top overflow-hidden" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-2xl mt-3 tracking-tighter"><%= user.name %></h3>
                    <h5  class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex gap-3 mt-10">
                        <a href="" class="text-zinc-400">Edit User</a><a href="/delete/<%= user._id %>" class="text-red-500 ">Delete User</a>
                    </div>
                </div> 
              <% }) %>
              <%} else {%>
                <h1 class="text-3xl">No Users Yet!</h1>
                <%}%>
                
            </div>
     </div>
</body>
</html>