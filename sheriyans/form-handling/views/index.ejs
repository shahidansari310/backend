<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Task</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- External CSS -->
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>

<body class="bg-zinc-900 text-white">

    <div class="flex justify-center items-center min-h-screen flex-col">

        <!-- Create Task Form -->
        <div class="w-[90%] md:w-1/2 lg:w-1/3 bg-zinc-800 p-8 rounded-xl shadow-xl">
            <form action="/create" method="post">
                <input class="block w-full px-5 py-3 bg-zinc-700 rounded-md outline-none focus:bg-zinc-600 transition"
                    type="text" placeholder="Title goes here" name="title">

                <textarea
                    class="mt-3 w-full px-5 py-3 bg-zinc-700 rounded-md resize-none outline-none focus:bg-zinc-600 transition"
                    placeholder="Write your task here" cols="30" rows="5" name="details"></textarea>

                <input
                    class="mt-4 w-full py-3 bg-blue-600 hover:bg-blue-700 transition cursor-pointer rounded-xl font-semibold"
                    type="submit" value="Create Task">
            </form>
        </div>

        <!-- Tasks List -->
        <div class="tasks w-5/6 mt-10 flex items-center gap-4 flex-wrap">
            <%if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <!-- Task Card 1 -->
                    <div class=" min-w-72 px-4 py-5 bg-zinc-800 rounded-lg">
                        <h1 class="text-xl font-semibold"><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-2 hover:underline" href="../files/">Read More</a>
                    </div>
                    <%})%>
                        <%} else{%>
                            <h3>No task Yet</h3>
                            <%}%>
        </div>

    </div>

    <script src="/javascript/script.js"></script>
</body>

</html>